export const logWarning = (msg) => {
  console.warn(`⚠️ ${msg} ⚠️`);
};

export const logError = (msg) => {
  console.error(`🚨 ${msg} 🚨`);
};

export const logHelp = (msg) => {
  console.log(`💡 ${msg} 💡`);
};
